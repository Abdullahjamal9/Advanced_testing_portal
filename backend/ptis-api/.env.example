# Backend environment variables
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=ptis_testing
PORT=3001
FRONTEND_URL=http://localhost:3000

# Optional: Cambria font paths (project-local folder)
# Use forward slashes to avoid escape issues on Windows.
PTIS_CAMBRIA_HEADINGS_FONT_PATH=D:/ptis-lms/backend/Fonts/cambriab.ttf
PTIS_CAMBRIA_FONT_PATH=D:/ptis-lms/backend/Fonts/cambria.ttf
